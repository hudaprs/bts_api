const initialState = {
  loading: false,
  isAuthenticated: true,
  token: null
}

export default (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state
  }
}
